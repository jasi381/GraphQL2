mutation InitiateSignOtpRequest(
    $site: String!,
    $device: EntitlementDevice!,
    $input: InitiatePasswordlessSignInput!)
{
    identityInitiateSignOtp(site: $site, device :$device, input: $input){
        key
    }
}